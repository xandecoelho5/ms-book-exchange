server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/xandecoelho5/ms-o-config-server
          default-label: main
          search-paths:
            - 'greeting-service*'
